<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Deliberately consume the published package, never a repository ProjectReference. -->
    <PackageReference Include="AutomationSandbox.SelfHealing" Version="0.2.0-beta.4" />
  </ItemGroup>

</Project>
